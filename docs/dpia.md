# Data Protection Impact Assessment

**Status:** Draft engineering input for legal and operational review.

This assessment describes the proposed production flow, not the current data set. The prototype processes simulated consultation data only. It must not accept real patient data until the open actions in this document, including the owner-assigned retention period, are closed.

This is not legal advice, a legal sign-off, or a substitute for review by Malaysian privacy counsel and the clinic that will act as data controller.

## Purpose And Scope

The proposed service turns an adult GP consultation transcript into a doctor-reviewed clinical note, missing-information prompts, red-flag candidates, and cited suggestions. It does not diagnose, prescribe, or approve a note automatically.

This assessment covers:

- **Data Subjects:** Patients, doctors, and account users.
- **Personal Data:** Account identity and contact data, session data, consultation transcripts, clinical notes, analysis, and audit identifiers.
- **Sensitive Data:** Consultation content and health information. Treat it as sensitive personal data even where direct identifiers have been removed.
- **Lifecycle:** Browser collection, API processing, database storage, model processing, audit logging, retrieval, access requests, and deletion.

## Plain-Language De-Identification Statement

**Removing identifiers does not anonymise health information. De-identification is risk reduction, not anonymisation.**

The model receives a tokenised transcript, not a safe anonymous dataset. Clinical facts, rare combinations, context, and a recipient's other information can still make a person identifiable or linkable. The original transcript, re-hydrated note, and analysis remain sensitive personal data at rest. Every production control in this document therefore treats de-identified content as protected health information.

## Production Data-Flow Map

The browser calls the API directly with cross-site cookies. The Vercel-hosted SPA does not proxy the consultation request. The API is deployed to Render Singapore, where Render's Cloudflare edge fronts requests. The service stores raw consultation data in Supabase Postgres Singapore and sends only tokenised transcript-derived content to the configured LLM endpoint.

| Step | Creation, Storage, Transmission, Or Processing                                                                                                                        | Data                                                                                                                             | Location Or Recipient                                                               | Existing Control Or Limitation                                                                                                                                            |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1    | A doctor signs in or creates an account in the browser.                                                                                                               | Name, email, password submission, session cookie.                                                                                | Doctor device, then Render API.                                                     | The application uses cookie sessions. `User`, `Account`, `Session`, and `Verification` data are stored in Postgres.                                                       |
| 2    | A doctor enters or reviews a transcript in the browser. Current inputs include pasted text and uploaded text. Local ASR is specified to keep audio in browser memory. | Full consultation text, which can include identifiers and health information.                                                    | Doctor device.                                                                      | No real data is permitted in the prototype. Local audio is not sent to the API.                                                                                           |
| 3    | The browser sends authenticated API requests directly to the Render API.                                                                                              | Account session, request metadata, transcript, edits, acknowledgements, and API responses.                                       | Render and its Cloudflare edge.                                                     | API ownership checks scope consultation access to the authenticated doctor.                                                                                               |
| 4    | The API creates and updates consultation records.                                                                                                                     | Raw transcript, analysis, edited note, status, timestamps, doctor relationship, red-flag acknowledgements, and reviewed gaps.    | Supabase Postgres Singapore.                                                        | The raw transcript is intentionally stored, not tokenised. There is currently no retention job or deletion endpoint.                                                      |
| 5    | The API authenticates and rate limits requests.                                                                                                                       | User identity, session token, optional IP address and user agent, password field in the account record, and rate-limit counters. | Supabase Postgres Singapore.                                                        | Authentication uses better-auth with database-backed session and rate-limit storage.                                                                                      |
| 6    | The API runs the deterministic red-flag engine and de-identification in process.                                                                                      | Raw transcript in process memory.                                                                                                | Render Singapore.                                                                   | Red-flag evaluation does not egress. De-identification detects names, NRIC, phone, address, birth date, record number, and email.                                         |
| 7    | The API creates a request-scoped vault and replaces detected values with stable tokens such as `[PATIENT_1]`.                                                         | Token-to-original mappings and tokenised transcript.                                                                             | Render process memory only.                                                         | The vault is neither persisted nor logged and is discarded when the request returns. Pattern detection can miss identifiers, especially unmarked names.                   |
| 8    | The LLM adapter re-scans the outbound tokenised content, then sends it for note and suggestion generation.                                                            | Tokenised transcript-derived content, system prompts, schema, and guideline identifiers.                                         | Alibaba Model Studio Singapore, default Qwen endpoint.                              | `DEID_FAIL_CLOSED=true` is required in production. The re-scan blocks payloads that still match a detector, but does not make the content anonymous.                      |
| 9    | The API validates the model response, re-hydrates tokens, and persists the reviewable result.                                                                         | Re-hydrated note, evidence, gaps, red flags, suggestions, and clinical facts.                                                    | Render process memory, then Supabase Postgres Singapore, then the doctor's browser. | Re-hydration occurs after response validation. The database result again contains identifiable health information.                                                        |
| 10   | The API records events and emits operational logs.                                                                                                                    | Audit IDs, actions, timestamps, detector labels, provider and model identifiers, request IDs, and limited failure categories.    | Supabase Postgres and Render log collection.                                        | Audit metadata and logger redaction prohibit transcript bodies, note contents, and vault entries. Platform request logs and support access still need contractual review. |
| 11   | A doctor retrieves, edits, acknowledges, approves, or reviews a consultation.                                                                                         | The stored consultation and audit history.                                                                                       | Supabase Postgres to Render, Cloudflare, and the doctor's browser.                  | Approved consultations are read-only. This is not a deletion or access-request mechanism.                                                                                 |

### Specified But Not Implemented Path

`asr_hosted` is specified, but no hosted ASR adapter is implemented or configured today. If built, it would send raw patient audio and voice data to Alibaba Model Studio Singapore before de-identification. It requires a separate processor review, a per-consultation consent interaction, and a DPIA update before use. Until then, hosted ASR must remain disabled.

## Processors, Sub-Processors, And Regions

Role labels below are the proposed contractual position. The clinic and operating entity must confirm controller and processor allocation in their agreements before real-data processing starts.

| Party                    | Proposed Role                                                                                                                             | Data In The Path                                                                                                                                             | Region And Reason                                                                                                                                                                        | Required Contract Or Verification                                                                                                                       |
| ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Clinic using the service | Data controller.                                                                                                                          | Patient and doctor data it enters or directs the service to process.                                                                                         | Malaysia. The clinic determines the care and recordkeeping purpose.                                                                                                                      | Confirm controller responsibilities, retention rule, patient notice, and request channel.                                                               |
| Service operator         | Data processor for the clinic, unless legal review finds another allocation.                                                              | All data the API processes for the clinic.                                                                                                                   | Malaysia and Singapore service path.                                                                                                                                                     | Execute a data-processing agreement and document personnel access controls.                                                                             |
| Vercel                   | Processor for static frontend delivery and platform request metadata, subject to its DPA.                                                 | Browser requests for SPA assets and associated platform metadata. The app sends clinical API calls directly to Render, not through Vercel.                   | Region is not declared in `vercel.json`. Vercel was chosen to host the static SPA separately from the API, reducing application handling of clinical content there.                      | Verify deployment, log, support, and sub-processor regions. Do not claim Malaysian or Singapore residency for this layer without evidence.              |
| Render                   | Processor for API hosting. Cloudflare is an infrastructure sub-processor on the API edge, as identified by the application configuration. | API requests, raw transcript while in memory, re-hydrated output while in memory, operational logs, and request metadata.                                    | Singapore, explicitly set in `render.yaml`. The region was chosen to keep the API in ASEAN and close to the database and default model endpoint.                                         | Execute DPA, confirm Cloudflare's role, support access, log retention, and all sub-processors.                                                          |
| Cloudflare               | Sub-processor of Render for API edge delivery, subject to Render's arrangements.                                                          | API traffic and edge request metadata. It can process request content when terminating or proxying TLS.                                                      | Not evidenced in this repository.                                                                                                                                                        | Obtain Render's current sub-processor list and regional handling terms before production.                                                               |
| Supabase                 | Processor for Postgres, Supavisor pooled connections, and database operations.                                                            | Auth records, sessions, rate limits, raw transcripts, notes, analyses, audit events, and database metadata.                                                  | Singapore, evidenced by `ap-southeast-1` in the configured connection host. The region was chosen to keep persistent health data in ASEAN near the API.                                  | Execute DPA, confirm backups, support access, log retention, deletion propagation, and sub-processors.                                                  |
| Amazon Web Services      | Potential infrastructure sub-processor of Supabase.                                                                                       | Supabase's connection hostname includes `aws-0-ap-southeast-1`, but the repository does not establish the contractual relationship or full processing scope. | Singapore region is indicated by `ap-southeast-1`.                                                                                                                                       | Verify through Supabase's current sub-processor documentation rather than treating the hostname as a complete assurance.                                |
| Alibaba Model Studio     | Processor for LLM inference, and prospective processor for hosted ASR if that specified adapter is built.                                 | Tokenised transcript-derived text for Qwen. Hosted ASR, if built, would receive raw audio.                                                                   | Singapore endpoint, configured as the default Qwen base URL. The region was chosen to keep the model endpoint in ASEAN and make the provider swappable if residency requirements change. | Execute DPA and transfer terms, confirm retention and training controls, support access, breach duties, and the exact Singapore processing commitment.  |
| Hugging Face CDN         | Not a current production processor. Specified browser-side model-delivery path for local ASR.                                             | Model-download request metadata and browser cache data. It does not receive audio or transcript content on the specified local-ASR path.                     | Region is not evidenced. The model is specified to download on first use and remain browser-cached so local transcription stays on the device.                                           | Verify delivery, logging, and sub-processor terms if the local-ASR implementation is released.                                                          |
| Google Gemini            | Not a production processor. Local-development provider for synthetic data only.                                                           | Synthetic local-development text only.                                                                                                                       | Google endpoint. The free tier permits use of submitted content for product improvement and human review.                                                                                | The production configuration rejects Gemini. It must never receive real or patient-derived data.                                                        |
| DeepSeek                 | Not a production processor. Benchmarking provider only.                                                                                   | Benchmark data outside production only.                                                                                                                      | PRC-hosted endpoint.                                                                                                                                                                     | The production configuration rejects DeepSeek because the PRC transfer question remains unresolved. It must never receive real or patient-derived data. |

The four chosen hosting locations are intentional: Vercel isolates static SPA delivery, Render Singapore hosts the API, Supabase Singapore persists data, and Alibaba Model Studio's Singapore endpoint performs default model inference. In-region ASEAN placement is a scored evaluation criterion, but it does not remove the need for a Malaysian cross-border transfer assessment.

## Malaysian PDPA Assessment

Health information and identifiers should be treated as sensitive personal data. The production design must be assessed under Malaysia's Personal Data Protection Act 2010, as amended, including the General, Notice and Choice, Disclosure, Security, Retention, Data Integrity, and Access Principles.

| PDPA Area                 | Engineering Assessment                                                                                                                                                | Required Pre-Production Action                                                                                                                                                                  |
| ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Purpose And Necessity     | The stated purpose is doctor-reviewed documentation and decision support within the defined consultation scope. Collection must be limited to what the service needs. | Define the clinic's permitted purpose and prohibit training, marketing, or secondary use unless separately assessed and authorised.                                                             |
| Sensitive Data Processing | A transcript, note, and analysis can reveal a person's health information.                                                                                            | Obtain legal advice on the applicable sensitive-data condition. Use clear, documented patient consent as the proposed baseline unless counsel approves and documents another basis.             |
| Notice And Choice         | The patient needs a clear written notice before processing.                                                                                                           | Provide the notice in Bahasa Malaysia and English. State data types, purposes, recipients, countries, retention, access channel, withdrawal consequences, and that a doctor reviews all output. |
| Disclosure                | Providers may receive data only for the declared purpose.                                                                                                             | Name processor categories and the Singapore LLM endpoint in the notice. Contractually prohibit independent use and onward disclosure.                                                           |
| Security                  | The database holds raw and re-hydrated health data. De-identification reduces LLM exposure but has imperfect recall.                                                  | Confirm encryption, least-privilege access, secrets management, support access, incident procedures, logging limits, and periodic de-identification testing.                                    |
| Retention And Integrity   | The current implementation persists consultation and audit rows indefinitely.                                                                                         | Implement the retention, deletion, correction, and backup-expiry design below before enabling real data.                                                                                        |
| Access And Correction     | The current authenticated consultation UI is not a formal patient access or correction path.                                                                          | Provide an identity-verified request route, controller decision workflow, response tracking, and correction audit trail.                                                                        |
| Cross-Border Transfer     | Transfers from Malaysia to Singapore occur at Render, Supabase, and Alibaba. Vercel and Cloudflare handling regions are not yet evidenced.                            | Complete a transfer impact assessment, record each receiver, country, data category, purpose, safeguards, and the chosen section 129 basis before production.                                   |

Official Malaysian guidance identifies the seven principles, requires notice and choice information, and describes access and correction rights. The cross-border guidance requires records showing each transfer and its section 129 basis. See the [PDPA Act 709](https://www.pdp.gov.my/ppdpv1/en/akta/pdp-act-2010-en/), the [privacy notice guide](https://www.pdp.gov.my/ppdpv1/wp-content/uploads/2025/01/A-Quick-Guide-to-PRIVACY-NOTICE-1.pdf), the [data-subject rights guidance](https://www.pdp.gov.my/ppdpv1/en/data-subject-rights/), and the [cross-border transfer guideline](https://www.pdp.gov.my/ppdpv1/wp-content/uploads/2025/08/GP_CBPDT_EN-1.pdf).

## Consent And Permitted Processing

The proposed operational baseline is explicit patient consent before a doctor submits any real consultation data. Consent must be voluntary, specific to the stated service, recorded, and withdrawable through the clinic's request channel. A patient must not be asked to consent to LLM training, marketing, or unrelated reuse as a condition of care.

Before a clinic relies on another permitted-processing basis or a sensitive-data exception, Malaysian counsel must identify and document it. Engineering must not infer a legal basis from the fact that the product is clinically useful or that a doctor has an account.

The consent and notice record must identify:

- **Controller And Contact:** The clinic, its privacy contact, and the service operator's role.
- **Purpose And Data:** Consultation documentation, reviewable decision support, the categories of health and account data, and no autonomous clinical decision.
- **Recipients And Transfers:** Render, Supabase, Alibaba Model Studio, and any confirmed Vercel, Cloudflare, or other sub-processor path.
- **Choices And Consequences:** Local-only or non-service alternatives where available, the hosted-ASR per-consultation choice if later implemented, and the consequences of withdrawal.
- **Rights Channel:** How the patient requests access, correction, withdrawal, deletion where permissible, or raises a complaint.

## Retention, Deletion, And Access Requests

### Open Retention Decision

The retention period is an owner-assigned decision. No number is set in this document and none may be inferred from the prototype.

**Recommended default:** Do not enable real-data processing until the clinic data controller has adopted a written retention period. That period should be no longer than the minimum clinical-record retention requirement applicable to the clinic's records, verified by Malaysian legal and professional-recordkeeping advice. This avoids inventing a period while preventing indefinite storage by default.

The decision must separately cover consultation data, account and session data, audit events, database backups, platform logs, and consent or request records. It must name the legal or operational rationale, the deletion trigger, and the accountable owner.

### Required Deletion Mechanism

No deletion or access-request endpoint exists today. Before production, implement and test a controller-authorised workflow that:

1. Verifies the requester's identity without placing new health information in ordinary support channels.
2. Locates the subject's consultation records, account records, sessions, audit records, and linked provider or platform records.
3. Applies the controller's retention decision, including any lawful requirement to retain a medical record or minimal audit evidence.
4. Deletes or irreversibly de-identifies eligible data in the primary database. The current `Consultation` to `AuditEvent` relation cascades on deletion, so the policy must explicitly decide which audit evidence, if any, is retained separately.
5. Expires copies from backups and platform logs on a documented schedule, with restoration controls that prevent deleted data from reappearing unnoticed.
6. Records request ID, decision, authority, scope, completion date, and exceptions without recording transcript or note content.
7. Obtains deletion confirmation and timing from each processor where its systems retain relevant data.

### Required Access And Correction Mechanism

Before production, the clinic must offer a documented request channel and service-level workflow for access, correction, withdrawal, and deletion requests. The workflow must:

- **Authenticate Safely:** Verify identity and authority, including a representative's authority where applicable.
- **Route To The Controller:** Require a clinic decision for medical-record correction, retention exceptions, and disclosure.
- **Provide A Bounded Export:** Return the permitted consultation and account data through a secure, authenticated channel.
- **Preserve Provenance:** Record corrections as an attributable correction or amendment, rather than silently overwriting a clinical record.
- **Track Deadlines And Outcomes:** Maintain a request register without clinical-content copies and escalate unresolved requests to the controller's privacy contact.

## Residual-Risk Register

| Risk                                                     | Residual Risk                                                                                                       | Mitigation                                                                                                                                                      | Owner                  |
| -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------- |
| De-identification misses an identifier.                  | Tokenised text can contain direct identifiers or remain re-identifiable through clinical context.                   | Fail closed on detector error, re-scan at LLM egress, request-scoped vault, testing with Malaysian identifiers, and no claim of anonymisation.                  | Engineering Lead       |
| Raw health data persists indefinitely.                   | Current database records have no retention or deletion path.                                                        | Keep synthetic-only scope, assign a retention policy, implement and test deletion, backup expiry, and processor deletion confirmation.                          | Clinic Data Controller |
| Malaysia-to-Singapore transfer lacks a documented basis. | Render, Supabase, and Alibaba processing are cross-border transfers.                                                | Transfer impact assessment, section 129 basis record, processor terms, notice, consent record where used, and annual re-review.                                 | Privacy Lead           |
| Platform or support access is broader than intended.     | Vercel, Render, Cloudflare, Supabase, Alibaba, and their sub-processors may hold metadata, logs, or support access. | DPA review, least privilege, access logging, support-access controls, sub-processor inventory, and contractual deletion commitments.                            | Vendor Manager         |
| Gemini is used with real data.                           | Google's free tier permits product-improvement and human-review use.                                                | Production boot guard, synthetic-only local-development policy, separate credentials, and configuration review.                                                 | Engineering Lead       |
| DeepSeek is enabled in production.                       | PRC processing introduces an unresolved cross-border transfer path.                                                 | Production boot guard, benchmarking-only policy, and deployment configuration review.                                                                           | Engineering Lead       |
| Hosted ASR sends raw audio before the gate.              | Voice and audio are direct identifiers and cannot be de-identified first.                                           | Keep adapter unbuilt and disabled, require separate DPIA update, named processor review, explicit per-consultation consent, and audit event before any release. | Product Owner          |
| Patient rights request is mishandled.                    | The current product UI is not a formal access, correction, or deletion process.                                     | Identity-verified request workflow, controller approval, request register, secure export, correction provenance, and staff training.                            | Clinic Data Controller |
| Browser, proxy, or platform logs expose metadata.        | IP address, user agent, URL, and support traces can identify a user even if clinical content is redacted.           | Confirm logging fields and retention with Vercel, Render, Cloudflare, and Supabase; minimise logs and restrict support access.                                  | Platform Lead          |
| Wrong controller or processor allocation.                | The clinic and service operator could make inconsistent notices, contracts, or rights decisions.                    | Legal review and signed role allocation before onboarding a clinic.                                                                                             | Privacy Lead           |

## Release Gate And Review Cycle

Real patient data remains out of scope until all of the following are evidenced:

- **Retention Approval:** The clinic data controller assigns the documented period and deletion exceptions.
- **Rights Workflow:** Access, correction, withdrawal, and deletion processes are implemented and tested.
- **Vendor Assurance:** DPAs, sub-processor lists, processing regions, deletion commitments, and support-access controls are reviewed.
- **Cross-Border Record:** The transfer impact assessment and section 129 basis are recorded for every non-Malaysian recipient.
- **Consent And Notice:** Patient-facing notice and consent record are approved for the operating clinic.
- **DPIA Review:** Malaysian privacy counsel and the clinic complete their review before production deployment and whenever a new processor, country, model provider, or hosted-ASR path is introduced.

Related implementation detail: [`trd.md` section 4](./trd.md#4-data-model-prisma), [`trd.md` section 9](./trd.md#9-de-identification-pipeline), [`trd.md` section 15](./trd.md#15-audit-logging), [`trd.md` section 17](./trd.md#17-environments--deployment), and [`trd.md` section 19, row 11](./trd.md#19-open-decisions-register).
