{
  "name": "evals",
  "private": true,
  "type": "module",
  "scripts": {
    "eval": "bunx tsx run.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@shared/types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "tsx": "^4.19.2",
    "typescript": "^5.9.2",
    "vitest": "^3.2.7"
  }
}
